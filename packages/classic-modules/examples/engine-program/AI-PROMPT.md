Continue this Engine source starter as my contribution. Read README.md, REVIEW.md, QUOTE-ENGINES.md, SOURCE-PINS.json, the actual source and existing tests first. Preserve the canonical constructor(Context,bytes), Context/Operation order, real request/fulfill/refund semantics and existing Module source API format. Reuse the existing configuration schema and binding mechanism; do not invent a new CLI, package language, privileged callback or API approval.

Use my explicit author wallet, author reward wallet and family salt. If they are unavailable, complete local source/tests with visibly marked fixtures and report the missing inputs. Never turn a fixture identity into a claim of wallet ownership. Do not place API keys or signing secrets in files. Source authors, reward wallets, launch creators, payers and beneficiaries have separate roles.

Implement my requested behavior with bounded money rights and complete dependencies. For a fixed parameter, enforce the same restriction in API configuration and the admitted contract/host path. Defaults must remain editable. A new quote CA does not automatically create a new reward family. Keep historical package bytes and identities intact.

Compile the actual source with the supplied profile, run tests relevant to the change, refresh the build reference and source inventory, and prepare the ordinary source submission through the existing CLI. Show the exact configuration ABI and proposed operator vectors. Report local tests, source intake, protected review, reviewer decision, registry admission and onchain/public availability separately. Do not sign, publish or submit externally without my authorization for that action.
