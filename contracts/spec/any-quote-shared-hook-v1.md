# Any Quote shared hook integration

Implementation contract, 10 September 2026. Not a deployment or approval receipt.

The authoritative product plan is the workspace artifact `outputs/any-quote-priority-20260910/DECISIONS.md`. This profile preserves existing Module Engine review, creator, revision and token identities, and adds an immutable shared V4 hook with an immutable LP engine per launch.

## Fixed interfaces

- `AnyQuoteTypesV1.Configuration` is the canonical 256-byte configuration. It binds schema, PoolManager address/runtime, shared hook, quote asset, signed initial tick, expiration and price evidence. The host checks the infrastructure, time and mathematical bounds. The price evidence is an offchain provenance commitment, not an oracle attestation validated by the contract. Website/API prepare approximately $5,000 FDV using trustworthy reference prices and bind the exact result into the user's launch intent. No silent repricing.
- Chain 4663, 1 billion tokens with 18 decimals, tick spacing 200, own LP fee zero. The new profile has its own schema and economics identity. Old fixed-configuration profiles retain their rules.
- `AnyQuoteSharedHookV1(IPoolManager manager, address host, address rewardAdmin)` creates `AnyQuoteLedgerV1(manager, host, rewardAdmin)` in its constructor. The ledger authenticates its deploying hook. The immutable host creates the shared hook using a mined CREATE2 salt; the deployment plan predicts the host's CREATE address from the actual signing wallet's nonce before mining. No post-deployment pointer setters or global Registry ownership transfer.
- To fit EIP-170 and EIP-3860 under the existing normal compiler, native-route validation is an immutable stateless dependency, `AnyQuoteNativeRouteGuardV1`, deployed separately. It has no constructor arguments and exposes pure `routerCallData(bytes operationData, address recipient, uint256 value, uint256 deadline) returns (bytes)`. It has no custody, approvals or owner. The host checks its exact runtime hash. Host constructor order: `(UERC20Factory tokenFactory, ClassicModuleLaunchPolicyV1 launchPolicy, IModuleEngineReviewAuthorityV1 registry, IPoolManager poolManager, address rewardAdmin, bytes32 hookSalt, AnyQuoteNativeRouteGuardV1 nativeRouteGuard)`. Getter `nativeRouteGuard()` and `NATIVE_ROUTE_GUARD_CODE_HASH()` join existing hook/ledger getters. Factory/policy runtime hashes use exact production constants instead of embedding full runtime bytecode in host initcode. Deployment address prediction uses the actual signing wallet's nonce, not an unrequested executor.
- `registerPool` uses `AnyQuoteTypesV1.PoolRegistration`. Only the immutable host registers; the hook passes the reward registration to its ledger. Pool registration precedes engine initialization atomically. Initialization checks the exact initializer, key and initial tick.
- The whole fixed 30 bps is credited to `0xD88539d3c4C460136a733A3Fd60cf6BF269079da`. Family/author identity remains review provenance; this new economic policy does not use the old 10/20 distribution. Creator buy/sell fees are separate, fixed on launch, 0 through 1,000 bps. Existing launch-policy 100-bps steps remain.
- Fee backing uses PoolManager ERC-6909 claims. The hook mints claims to the ledger after checking actual Core deltas, then accrues the exact fees. Later claims burn ERC-6909 and transfer the same quote asset. No token transfers, reward-recipient callbacks or Registry lookups during accrual.
- Carry is isolated by pool and buy/sell direction. Two separately rounded fees share the gross quote basis. Exact-output inversion uses a bounded candidate search; the net function is not assumed monotone. Carry is committed exactly once in `afterSwap`.
- The hook allows normal external V4 routers without privileged hook data. Flags: beforeInitialize, beforeSwap, afterSwap, beforeSwapReturnDelta and afterSwapReturnDelta. The hook has no function that calls PoolManager.swap itself. Reject partial fills consistently and expose this limit in route preparation.
- `AnyQuoteLPModuleV1` retains the canonical `ModuleEngineTypesV1.Context` constructor and `IModuleEngineV1` lifecycle, but is not a hook. In this new profile `Context.feeCollector` equals the shared hook's ledger. It owns one permanently locked LP position, exposes its pool identity and has no remove/rescue/mint/upgrade path. Optional host exact-input swaps are convenience entrypoints; fees remain inside the pool callbacks. Quote decimals 0 through 36 are admitted when tick and amount ranges are representable. The signed tick must be strictly inside usable extremes and aligned to 200; initial liquidity must fit V4's per-tick bound.
- `ModuleEngineAnyQuoteHostV1` retains existing revision binding and `EngineLaunchBound`/`EngineLaunchParametersBound` signatures. Initial buy is optional. Native initial buy uses operation ID `keccak256("spot.buy.native-exact-input.v1")` in the existing `T.Operation` envelope, with `data = abi.encode(bytes commands, bytes[] inputs)`. It must execute atomically after initialization via the pinned Universal Router, bind actor/recipient/final minimum/deadline and reject excess funding. Explicit user recipients, not the Router MSG_SENDER sentinel, prevent sending purchased tokens to the host. Only validated synchronous AMM commands are allowed; no allow-revert, bridge, offchain order, permit or arbitrary external target. No host-only fee collection, no hidden extra native fee.
- Preserve `claimQuoteTo`, `claimQuoteFor`, `claimableQuote`, creator recipients and future-only recipient rotation. Platform recipient is initially fixed by this version; only its own beneficiary or the explicit reward admin may redirect future base credits. Existing credits remain at the old wallet. Recipient rotation must not affect swap validity.

## Routing identity

Use the existing production Chain 4663 profile: Universal Router 2.1.1 at `0x06AfBA43Fd06227fA663b0DAecF536f6EaA6bf99`. Installed SDK address lookup currently returns a superseded deployment and must not choose the router. Compose validated external AMM paths with the known new V4 PoolKey; exclude asynchronous order routes. Uniswap's API discovers the external leg. Complete calldata and final minimum output must be verified before signing.

The delivered route compiler is a preparation foundation, not production admission. Its mixed-hop `CONTRACT_BALANCE`/`SWEEP` encoding still needs enforceable isolation from preexisting router balances. A `balanceAccounting` field, a currently empty router or nominal donation subtraction does not provide that isolation. Do not activate that route before this is resolved. For all-V4 paths, prefer one `SWAP_EXACT_IN` path within one PoolManager unlock, exact initial settlement and final scoped credit payout, reusing V4Planner. Mixed V2/V3 paths require actual amount/refund isolation using existing coordinator/settlement building blocks. A zero-router-balance gate would permit trivial denial of service through donations and is not acceptable.

The authenticated Trading API key is not present in the inspected local or production configuration. Production has an existing Uniswap graph adapter and subgraph credential, which are distinct from Trading API access. Reuse that existing discovery provider via the validated discovery adapter if appropriate; do not fabricate a key or infer broad readiness from a fixed pair. The existing main Programmable pool has a successful read-only stateful buy/approval/sell proof; it does not establish the unpublished module lifecycle.

## Events

Preserve canonical Engine launch and operation events. Add `SharedQuotePoolBound` with indexed poolId, launchId and token plus quoteAsset, engine, revisionId, familyId, configurationHash, initialTick and creator fee rates. Add `QuotePoolSwap` with indexed poolId, launchId and swapSender plus buy, exactInput, grossQuote, platformQuote, creatorQuote and Core amount0/amount1. `swapSender` identifies a router or engine, not necessarily the user. Existing claim/recipient events remain available to the indexer.

## Release boundaries

Contracts, protected review, catalog, source indexing, route/price preparation, frontend and deployment bindings must agree on this version before activation. The user's latest instruction is to sign the actual transactions himself at the end. Use the existing manual Owner publication path and do not build new executor, permit or Registry-delegation infrastructure for this release. Registry family registration still requires its current owner. The live canary pairs with the canonical Programmable token on Robinhood Chain, verified from production data. Funding and finality evidence are separate from local tests.

Local core verification at the handoff: 63 Solidity tests and 11 route/price tests pass. Host runtime is 23,825 bytes and complete initcode is 49,137 bytes including its seven static arguments, only 15 bytes below EIP-3860. Any host/hook/ledger/math change requires a renewed size check; do not raise EVM limits to bypass it. Native-route guard runtime hash is `0x704f8f3c1903e1f15dd041b2dd29673a8c98bee9c87f645da90978c2315cf4ee`. These local checks do not replace protected review or public release evidence.
